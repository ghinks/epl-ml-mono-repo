{
  "name": "root",
  "private": false,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
    "lerna-version": "lerna version --conventional-commits --yes",
    "pretest": "lerna bootstrap",
    "test": "lerna run test"
  }
}
