const url = "mongodb://localhost:27017";
const dbName = "epl-scores";
const historicalMatches = "matches";
const fixtures = "fixtures";
const fixturesUrl =  { host: "http://localhost:3000", uri: "/fixtures" }
const numAllTimeTeams = 43;

export { url, dbName, historicalMatches, fixtures, fixturesUrl, numAllTimeTeams };
